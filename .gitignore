# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Firmware compile staging (Main.ino is copied here to satisfy arduino-cli's
# sketch folder naming rule; the copy is a build artifact, not source)
build/

# Firmware secrets (Wi-Fi credentials, backend URL). Commit only the example.
arduino_secrets.h

# Runtime notification log written by the default notifier backend.
notifications.log

# Firebase project config (copy from firebase.config.example.js). Not committed.
firebase.config.js

# OS cruft
.DS_Store
Thumbs.db
